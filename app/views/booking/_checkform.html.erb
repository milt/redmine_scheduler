<%= form_tag({:controller => "booking", :action => "index"}, :method => "post", :class => "nifty_form") %>

	<% @skills.each do |skill| %>
		<%= label_tag skill.name %>
	  	<%= check_box_tag 'skill_ids[]', skill.id, @filter.include?(skill.id.to_s) %>
	<% end %>

<%= submit_tag 'Update' %>
