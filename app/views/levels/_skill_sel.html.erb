<script>
function authorizationratings(){
  var selected_id = document.getElementById('skill_id').selectedOptions;
}
</script>

<% form_for :level, @level, :url => { :action => "create" }, :html => {:class => "nifty_form"} do |f| %>
  <%= f.select :user_id, @users.map {|u| [u.name, u.id] } %>

  <% @skills.each do |skill| %>
    <% if !skill.name.nil? %>
      <%= check_box_tag :skill_id, skill.id %>
      <%= skill.name %>
    <%end%>
  <% end %>


  <%= submit_tag "Create" %>
<% end %>
