<script>
function authorizationratings(){
	/*var skillid = document.getElementById("skill_id");
	if (skillid.value = 1){
		//alert("authorization selected")
		//alert(document.getElementById("rating").attributes)
	}else{
		alert("not authorization")
	}*/
}
</script>

<% form_for :level, @level, :url => { :action => "create" }, :html => {:class => "nifty_form"} do |f| %>
  <%= f.select :user_id, @users.map {|u| [u.name, u.id] } %>
  <%= select_tag (:skill_id, options_for_select(@skills_list, @skill_choice), :onchange=>"authorizationratings()") %>
  <%= f.select :rating, 1..3 %>
  <%= submit_tag "Create" %>
<% end %>
