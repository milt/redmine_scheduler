%h2 New Fine

#fine-form
  = render 'form'
- if @repair.present?
  #fine-repair
    %h3 Associated Repair:
    = render 'repairs/repair', repair: @repair

- content_for :header_tags do
  - stylesheet_link_tag 'fines', :plugin => 'redmine_scheduler'