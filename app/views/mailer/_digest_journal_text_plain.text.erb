  <%= journal.created_on.strftime("%b %e, %l:%M %p") %> by <%= journal.user.name %>:
<% details_to_strings(journal.details, true).each do |string| -%>
<%= string %>
<% end -%><% if journal.notes.present? %>    - Comment: <%= journal.notes %><% end %>