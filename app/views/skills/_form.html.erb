<% form_for :skill, @skill, :url => { :action => "create" }, :html => {:class => "nifty_form"} do |f| %>
  <%= f.text_field :name %>
  <%= f.select :skillcat, Skillcat.all.collect {|cat| cat.name} %>
  <%= submit_tag "Create" %>
<% end %>
