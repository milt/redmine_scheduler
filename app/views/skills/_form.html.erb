<% form_for :skill, :url => { :action => "create" }, :html => {:class => "nifty_form"} do |f| %>
  <%= f.text_field :name %>
  <%= f.select :skillcat_id, Skillcat.all.collect {|s| [ s.name, s.id ] } %>
  <%= submit_tag "Create" %>
<% end %>
