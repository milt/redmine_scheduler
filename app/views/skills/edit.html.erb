<h1>Edit Skill</h1>

<% form_for @skill, :url => { :action => "update", :id => @skill[:id] } do |f| %>
  <%= f.text_field :name, :value => @skill.name %>
  <%= f.select :skillcat_id, Skillcat.all.collect {|s| [ s.name, s.id ]}, :selected => @skill.skillcat_id %>
  <%= submit_tag "Update" %>
<% end %>

<p>Make your desired changes and click "Update" to commit them. Note that this skill will automatically update in the per user assignment list.</p>

<h3>Assigned Users</h3>
<% @assigned.each do |user| %>
  <p>
  <%= user[:firstname] %> <%= user[:lastname] %> : <%= link_to 'Remove', {:action => 'unlink', :id => @skill[:id], :user_id => user[:id]} %>
  </p>
<% end %>
<br>
<h3>Unassigned Users</h3>
<% @unassigned.each do |user| %>
  <p>
  <%= user[:firstname] %> <%= user[:lastname] %> : <%= link_to 'Add', {:action => 'link2', :id => @skill[:id], :user_id => user[:id]} %>
  </p>
<% end %>
<br>
<%= link_to 'Cancel and return to Skill Index', { :action => 'index' } %>

