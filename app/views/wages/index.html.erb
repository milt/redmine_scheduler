<h2>Wages</h2>

<ul>
<% @users.each do |user| %>
<li>
	<%= user.firstname %> : <%= user.wage.amount rescue "No wage" %> : <%= collection_select(:user, :wage_id, @wages, :id, :amount) %>
</li>
<% end %>

</ul>