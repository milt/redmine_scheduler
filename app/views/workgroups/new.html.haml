= render 'nav/nav'
%h2
  = link_to l(:label_group_plural), groups_path
  » #{l(:label_group_new)}
- form_for(@group, :builder => TabularFormBuilder, :lang => current_language) do |f|
  = render :partial => 'form', :locals => { :f => f }
  %p= f.submit l(:button_create)
