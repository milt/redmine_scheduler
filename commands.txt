

Useful commands 


Class   # lists all params
Class.all   # lists all instances of Class
Class.where(name:"").first
 = Class.find(:all,:conditions=>[]).first


to remove a plugin's database:
>>rake redmine:plugins:migrate NAME=redmine_scheduler VERSION=0

to remove redmine database:
>>rake db:drop

